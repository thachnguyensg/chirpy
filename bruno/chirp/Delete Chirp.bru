meta {
  name: Delete Chirp
  type: http
  seq: 12
}

delete {
  url: {{host}}/api/chirps/{{chirp_id}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "body": "hahahahahah {{$randomAlphaNumeric}}"
  }
}

vars:post-response {
  chirp_id: res.body.id
}

settings {
  encodeUrl: true
  timeout: 0
}
