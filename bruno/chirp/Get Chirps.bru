meta {
  name: Get Chirps
  type: http
  seq: 6
}

get {
  url: {{host}}/api/chirps
  body: none
  auth: inherit
}

body:json {
  {
    "body": "hahahahahah {{$randomAlphaNumeric}}",
    "user_id": "{{user_id}}"
  }
}

vars:post-response {
  chirp_id: res.body[0].id
}

settings {
  encodeUrl: true
  timeout: 0
}
