meta {
  name: Login
  type: http
  seq: 5
}

post {
  url: {{host}}/api/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "{{user_email}}",
    "password": "{{password}}"
  }
}

vars:post-response {
  user_id: res.body.id
}

settings {
  encodeUrl: true
  timeout: 0
}
