meta {
  name: Revoke token
  type: http
  seq: 8
}

post {
  url: {{host}}/api/revoke
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{refresh_token}}
}

body:json {
  {
    "body": "hahahahahah {{$randomAlphaNumeric}}",
    "user_id": "{{user_id}}"
  }
}

vars:post-response {
  token: res.body.token
}

settings {
  encodeUrl: true
  timeout: 0
}
