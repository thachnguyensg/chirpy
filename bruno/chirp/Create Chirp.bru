meta {
  name: Create Chirp
  type: http
  seq: 8
}

post {
  url: {{host}}/api/chirps
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "body": "hahahahahah {{$randomAlphaNumeric}}",
    "user_id": "{{user_id}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
