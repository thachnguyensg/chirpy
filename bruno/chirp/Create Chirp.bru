meta {
  name: Create Chirp
  type: http
  seq: 6
}

post {
  url: {{host}}/api/chirps
  body: json
  auth: inherit
}

body:json {
  {
    "body": "hahahahahah {{$randomAlphaNumeric}}",
    "user_id": "{{user_id}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
