meta {
  name: Create Chirp
  type: http
  seq: 9
}

post {
  url: {{host}}/api/chirps
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "body": "hahahahahah {{$randomAlphaNumeric}}"
  }
}

vars:post-response {
  chirp_id: res.body.id
}

settings {
  encodeUrl: true
  timeout: 0
}
