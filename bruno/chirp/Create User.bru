meta {
  name: Create User
  type: http
  seq: 4
}

post {
  url: {{host}}/api/users
  body: json
  auth: inherit
}

body:json {
  {
    "email": "hehe{{$randomInt}}@hehe.com"
  }
}

vars:post-response {
  user_id: res.body.id
}

settings {
  encodeUrl: true
  timeout: 0
}
